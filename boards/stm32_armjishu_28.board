/* BOARD_START(_desc) */
BOARD_START("'ARMJishu' 28'' STM32F103RB based board")

/* DEFAULT_CONSOLE */
DEFAULT_CONSOLE(UART_RES(USART_PORT1))

/* LED(gpio) */
LED(GPIO_RES(PA2))
LED(GPIO_RES(PA3))
LED(GPIO_RES(PB2))

/* MMC_PARAMS(_spi_port, _mosi, _cs) */
MMC_PARAMS(SPI_RES(SPI_PORT1), GPIO_RES(PA7), GPIO_RES(PB7))

BOARD_END()
