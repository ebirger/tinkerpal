/* BOARD_START(_desc, _chipset, _image) */
BOARD_START("EK TM4C1294XL (Tiva C Connected Launchpad)", tm4c1294, NA)

/* DEFAULT_CONSOLE */
DEFAULT_CONSOLE(UART_RES(UART0))

/* LED(gpio) */
LED(GPIO_RES(PF0))
LED(GPIO_RES(PF4))
LED(GPIO_RES(PN0))
LED(GPIO_RES(PN1))

/* SSD1306_PARAMS(_i2c_port, _i2c_addr) */
SSD1306_PARAMS(I2C_RES(I2C1), 0x78)

BOARD_END()
