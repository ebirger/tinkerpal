/* BOARD_START(_desc, _chipset) */
BOARD_START("EK LM3S6965", lm3s6965)

/* DEFAULT_CONSOLE */
DEFAULT_CONSOLE(UART_RES(UART0))

/* MMC_PARAMS(_spi_port, _mosi, _cs) */
MMC_PARAMS(SPI_RES(SSI0), GPIO_RES(PA5), GPIO_RES(PD0))

/* SSD1329_PARAMS(_rst, _cs, _cd, _spi_port) */
SSD1329_PARAMS(GPIO_RES(PC6), GPIO_RES(PA3), GPIO_RES(PC7), SPI_RES(SSI0))

BOARD_END()
