/* BOARD_START(_desc, _chipset, _image) */
BOARD_START("CC3200-LAUNCHXL (CC3200 Connected Launchpad)", cc3200, NA)

/* DEFAULT_CONSOLE */
DEFAULT_CONSOLE(UART_RES(UART0))

/* LED(gpio) */
LED(GPIO_RES(PB1))
LED(GPIO_RES(PB2))
LED(GPIO_RES(PB3))

BOARD_END()
