/* BOARD_START(_desc, _chipset) */
BOARD_START("Unix based simulator", NA)

/* DEFAULT_CONSOLE */
DEFAULT_CONSOLE(UART_RES(STDIO_ID))

/* SDL_SCREEN_PARAMS(_width, _height) */
SDL_SCREEN_PARAMS(320, 200)

BOARD_END()
